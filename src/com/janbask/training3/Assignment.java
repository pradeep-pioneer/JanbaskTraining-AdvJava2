package com.janbask.training3;

public class Assignment {
    //ToDo: Add code to insert CLOB data into the table by downloading HTML from any webpage (HINT: BLOB stream would be passed as empty, we would only read data for clobData parameter).
}
